# Firebase Configuration
# Copy this file to .env.local and fill in your Firebase project credentials
# Get these values from Firebase Console > Project Settings > Your Apps

REACT_APP_FIREBASE_API_KEY=your_api_key_here
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
REACT_APP_FIREBASE_APP_ID=your_app_id

# GitHub Pages Deployment
# Set in GitHub Actions Secrets for CI/CD build
# https://github.com/JamesHD001/GP/settings/secrets/actions

# Optional: For Sentry error tracking (if implemented)
# REACT_APP_SENTRY_DSN=https://...

# Optional: API endpoints (if backend is added later)
# REACT_APP_API_BASE_URL=https://api.example.com

# Usage:
# - Local development: Copy to .env.local and fill values (not committed)
# - GitHub Actions: Set values in repository Secrets
# - Build command: npm run build (uses these env vars)
